const AddTodo = () => {
  return (
    <form className="flex flex-row">
      <input className="grow" placeholder='New todo'/>
      <button>Submit</button>
    </form>
  );
};

export default AddTodo;
